import React from 'react';
import {View, Text} from 'react-native';

const ServiceScreen = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text style={{fontSize: 24, color: 'blue'}}>Service Screen</Text>
    </View>
  );
};

export default ServiceScreen;